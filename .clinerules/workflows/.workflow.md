# Development Workflow

**Update**

```
Resume: | Rationalize | Resolve | Refine | Review |

1. Update the status of the task document @/.task.md , @/docs/amazon-seller-tools/documentation.md, @/src/app/content/blog/amazon-seller-tools.mdx and @/src/components/amazon-seller-tools/WhatsNewModal.tsx based on working/latest @git-changes. Proceed to step 2 if no significant changes.
2. Address these problems @problems @/typecheck-output.txt if no errors and warnings available, proceed to step 3.
3. Continue the pending implementation outlined on @/.task.md
4. Find potential bugs and opportunities for improvements using static analysis tools (e.g., ESLint, SonarQube) and code review checklists.
5. @git-changes Use git command to push changes and create meaningful commit messages.
```

**Code Checking**

```
Execute the command `npm run check`. Upon encountering a non-zero exit code (indicating failure), meticulously parse the command's output stream (both stdout and stderr) to pinpoint the root causes of the check failure. Dynamically adjust your execution based on identified error types: automatically fix identified linting or formatting errors using relevant npm scripts or commands (e.g., `npm run lint:fix`, `npm run format`). Attempt to automatically resolve type-checking errors by suggesting or implementing minor code modifications if a pattern is detectable and the change is non-breaking. If automated resolution is impossible, clearly present actionable insights with specific file paths, line numbers, and error messages extracted from the output to facilitate efficient manual debugging. Record all automated fixes attempted and any errors that could not be resolved. If the `npm run check` command includes security audits, address reported vulnerabilities by automatically updating vulnerable dependencies to their latest stable versions, prioritizing updates that minimize breaking changes. After applying automated fixes, re-execute `npm run check` to confirm issue resolution and iterate on this process until either all issues are resolved or further automated remediation is deemed infeasible. Log all actions taken and the final status (success or unresolved issues) to a file for review.
```

**Build**

```
Execute the `npm run build` command. If the build fails (exits with a non-zero code), analyze the output to identify and resolve the issues.
```

**Run**

```
1. Execute: npm run dev
2. Analyze the web page at `http://localhost:3000/` for usability, accessibility (using tools like WAVE or Axe), performance (using browser developer tools), and design. Provide specific, actionable recommendations for improvement, including code snippets or examples where applicable. Prioritize suggestions based on impact and feasibility, and categorize them by area (e.g., UI/UX, SEO, Code Optimization).
```

**Pages**

```
- `/` (Home Page)
- `/academy` (Academy Page)
- `/amazon-seller-tools` (Amazon Seller Tools Page)
- `/ats` (ATS Page)
- `/blog` (Blog Page)
- `/chat` (Chat Page)
- `/crm` (CRM Page)
- `/project-management` (Project Management Page)
- `/workflow-builder` (Workflow Builder Page)
```

**Rationalize**

```
Develop and execute a comprehensive action plan to integrate suggested improvements, ensuring minimal disruption to existing functionality and adherence to best practices.
```

**Resolve**

```
Implement the changes outlined in the action plan, focusing on code quality, maintainability, and scalability.
```

**Refine**

```
Analyze the Amazon Seller Central dashboard and related tools to identify actionable, incremental improvement opportunities specifically designed to enhance usability, streamline workflows, or provide clearer insights, focusing on changes that can be implemented in small, committable steps.

Reference:
@/.task.md
@/docs/amazon-seller-tools/documentation.md
@git-changes
```
