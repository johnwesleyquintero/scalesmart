---
title: API Reference
slug: api-reference
description: Detailed documentation for our API endpoints.
category: api
order: 2
---

# API Reference

This section provides comprehensive details on how to interact with our RESTful API.

## General API Structure

All API endpoints are prefixed with `/api/`. Requests and responses are typically in JSON format.

### Authentication

All API requests must be authenticated using an API key. Include your API key in the `Authorization` header as a Bearer token:

```
Authorization: Bearer YOUR_API_KEY
```

### Error Handling

API errors are returned with appropriate HTTP status codes and a JSON body containing an `error` message and a `code` for specific error types.

```json
{
  "error": "Unauthorized",
  "code": "AUTH_001"
}
```

## Available API Endpoints

Below is a summary of the main API categories. For detailed documentation on each endpoint, refer to the specific `.mdx` files within the `api/` directory.

- **Module Management:** Endpoints for tracking module progress.
  - [`/api/module-progress`](/docs/api-module-progress)
- **Authentication:** Endpoints for user authentication and session management.
  - [`/api/auth/nextauth`](/docs/api-auth-nextauth)
  - [`/api/auth/nextauth-options`](/docs/api-auth-nextauth-options)
- **Contact:** Endpoint for submitting contact form data.
  - [`/api/contact`](/docs/api-contact)
- **Content Management:** Endpoints for dynamic content.
  - [`/api/content`](/docs/api-content)
- **Download:** Endpoint for file downloads.
  - [`/api/download`](/docs/api-download)
- **Prohibited Keywords:** Endpoints for managing a list of prohibited keywords.
  - [`/api/prohibited-keywords`](/docs/api-prohibited-keywords)
- **Redis:** Endpoints for Redis cache interactions.
  - [`/api/redis`](/docs/api-redis)
- **Search:** Endpoints for search functionality.
  - [`/api/search`](/docs/api-search)
- **API Cache:** Endpoints for managing API caching.
  - [`/api/api-cache`](/docs/api-api-cache)

This reference provides a high-level overview. For detailed request/response schemas, parameters, and examples, please navigate to the specific API documentation links provided above.
